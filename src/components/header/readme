// Пример передачи пропсов.

// Компонент имеет один пропс, в который принимает булево значение (true или false), от которого зависит, каким будет хедер, прозрачным или обычным.

// Пример:

 <Header transparent={true} />
